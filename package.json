{
  "name": "storyboard-v4-admin",
  "private": true,
  "version": "4.1.5",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check worker.js && npm test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
